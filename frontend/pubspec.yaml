name: student_registration_app
description: A simple student registration app (university project).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.0 # 👈 Міне, осы жолды қостық!

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true